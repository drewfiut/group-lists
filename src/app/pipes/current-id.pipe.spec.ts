import { CurrentIDPipe } from './current-id.pipe';

describe('CurrentIDPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentIDPipe();
    expect(pipe).toBeTruthy();
  });
});
